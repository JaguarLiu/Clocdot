version: '3.8'

services:
  postgres:
    image: postgres:17
    container_name: clocdot-postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: passwrd
      POSTGRES_DB: db
    ports:
      - "5433:5432"
    volumes:
      - postgres-dev:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: clocdot-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-dev:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    image: asia-east1-docker.pkg.dev/flashesim/clocdot/server:latest
    container_name: clocdot-server
    environment:
      DATABASE_URL: postgresql://user:password@postgres:5432/clocdot_db?schema=public
      JWT_SECRET: dev-secret-change-in-production
      PORT: 3000
    ports:
      - "3000:3000"
    depends_on:
      - postgres
      - redis
    restart: unless-stopped

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    image: asia-east1-docker.pkg.dev/flashesim/clocdot/client:latest
    container_name: clocdot-client
    ports:
      - "80:80"
    depends_on:
      - server
    restart: unless-stopped

  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile
    image: asia-east1-docker.pkg.dev/flashesim/clocdot/admin:latest
    container_name: clocdot-admin
    ports:
      - "8080:80"
    depends_on:
      - server
    restart: unless-stopped

volumes:
  postgres-dev:
  redis-dev:
