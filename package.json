{
  "name": "checkpoint",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"yarn workspace @checkpoint/client dev\" \"yarn workspace @checkpoint/server dev\"",
    "build": "yarn workspace @checkpoint/client build",
    "dev:client": "yarn workspace @checkpoint/client dev",
    "dev:server": "yarn workspace @checkpoint/server dev",
    "db:migrate": "yarn workspace @checkpoint/server db:migrate",
    "db:push": "yarn workspace @checkpoint/server db:push",
    "db:studio": "yarn workspace @checkpoint/server db:studio"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
